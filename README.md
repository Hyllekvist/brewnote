# Coffee & Tee — SEO-first Coffee/Tea app scaffold (Next.js + Vercel + Supabase)

## Run
```bash
npm i
cp .env.example .env.local
npm run dev
```

## Deploy
- Push to GitHub → import into Vercel
- Add env vars from `.env.example`
- Create Supabase project + run SQL in `supabase/migrations/`
